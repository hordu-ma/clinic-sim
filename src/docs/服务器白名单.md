# A/B 服务器出站白名单（生产部署）

> 适用场景：A 为入口层（Nginx + 前端静态），B 为核心层（FastAPI + vLLM + PostgreSQL + MinIO）。
> 说明：A 服务器为 117 lenovo; B 服务器为 123 amax

## 一、B 服务器（核心层）

### 1. 系统与容器安装（APT + Docker + NVIDIA）

**出站端口：TCP 80/443**

- Ubuntu APT 源（国内镜像，任选其一即可）：
  - mirrors.tuna.tsinghua.edu.cn
- Docker APT 源（国内镜像）：
  - mirrors.tuna.tsinghua.edu.cn
- NVIDIA 容器工具链：
  - nvidia.github.io
  - packages.nvidia.com

### 2. 镜像拉取（Docker 国内镜像）

**出站端口：TCP 443**

- docker.mirrors.ustc.edu.cn

> 若使用其它国内镜像，请替换为对应域名。

### 3. 模型拉取（ModelScope）

**出站端口：TCP 443**

- modelscope.cn
- www.modelscope.cn

---

## 二、A 服务器（入口层）

### 1. 系统与容器安装（APT + Docker + Nginx）

**出站端口：TCP 80/443**

- Ubuntu APT 源（国内镜像）：
  - mirrors.tuna.tsinghua.edu.cn
- Docker APT 源（国内镜像）：
  - mirrors.tuna.tsinghua.edu.cn

### 2. 镜像拉取（Docker 国内镜像）

**出站端口：TCP 443**

- docker.mirrors.ustc.edu.cn
